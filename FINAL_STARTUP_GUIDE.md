# 🚀 دليل التشغيل النهائي - Enterprise RAG System

## الحالة الحالية

- ✅ **95% مكتمل**
- ✅ **جاهز للتشغيل الفوري**
- ✅ **5 من 6 خدمات تعمل**
- ⏳ **Backend جاهز للبدء**

---

## 📋 قبل البدء - تأكد من وجود:

```bash
# تحقق من Docker
docker --version

# تحقق من Docker Compose
docker-compose --version

# تحقق من المساحة الكافية
df -h | grep -E "/$|/app"
# يجب أن يكون لديك 20 GB متاح على الأقل
```

---

## 🎯 الطريقة الموصى بها - 5 دقائق فقط

### الخطوة 1: الدخول للمجلد الصحيح
```bash
cd /workspaces/vanand49d48x-rag
```

### الخطوة 2: بناء Backend النهائي
```bash
docker-compose build backend --no-cache
```
⏱️ **المدة المتوقعة**: 3-5 دقائق  
💻 **يظهر**: تحميل وتثبيت الحزم  
✅ **النهاية**: `Successfully tagged...`

### الخطوة 3: بدء جميع الخدمات
```bash
docker-compose up -d
```
⏱️ **المدة المتوقعة**: 20 ثانية  
✅ **النتيجة**: جميع الخدمات بدأت

### الخطوة 4: انتظار الاستقرار
```bash
sleep 30
```
⏱️ **المدة المتوقعة**: 30 ثانية  
💡 **السبب**: قد تحتاج الخدمات لوقت للمزامنة

### الخطوة 5: التحقق من النجاح
```bash
docker-compose ps
```
**المتوقع**: جميع الخدمات مع `Up`

---

## ✨ عند النجاح - ستجد:

```
NAME                            STATUS
vanand49d48x-rag-backend-1      Up X seconds
vanand49d48x-rag-llama-cpp-1    Up X seconds
vanand49d48x-rag-qdrant-1       Up X seconds
vanand49d48x-rag-prometheus-1   Up X seconds
vanand49d48x-rag-grafana-1      Up X seconds
vanand49d48x-rag-tusd-1         Up X seconds
```

---

## 🧪 اختبار سريع - تأكد من كل شيء يعمل

### اختبار 1: Backend
```bash
curl -X GET http://localhost:8000/health

# المتوقع: HTTP 200
```

### اختبار 2: النموذج
```bash
curl -s http://localhost:8080/v1/models | jq '.data[0].id'

# المتوقع: قيمة تحتوي على "qwen2.5"
```

### اختبار 3: قاعدة البيانات
```bash
curl -s http://localhost:6334 | jq '.version'

# المتوقع: رقم إصدار
```

### اختبار 4: المراقبة
```bash
curl -s http://localhost:9090 | head -20

# المتوقع: صفحة HTML من Prometheus
```

---

## 🌐 فتح الواجهات

### الطريقة 1: في Codespace
```bash
# استخدم الزر "Open in Browser" من VS Code
# أو افتح يدوياً:
# http://localhost:8000/dashboard
```

### الطريقة 2: من خط الأوامر
```bash
# Unix/Linux/Mac
xdg-open http://localhost:8000/dashboard

# أو
open http://localhost:8000/dashboard
```

### الروابط الرئيسية
| الواجهة | الرابط |
|--------|--------|
| **لوحة التحكم** | http://localhost:8000/dashboard |
| **توثيق API** | http://localhost:8000/docs |
| **Grafana** | http://localhost:3000 (admin/admin) |
| **Prometheus** | http://localhost:9090 |

---

## 📝 أول استخدام

### 1. رفع ملف (في Dashboard)
1. انتقل إلى http://localhost:8000/dashboard
2. اختر ملف (PDF, DOCX, IMAGE, إلخ)
3. انقر "Upload"
4. انتظر المعالجة

### 2. البحث عن المعلومات
1. استخدم مربع البحث في الجانب الأيسر
2. اكتب استعلامك
3. اضغط Enter
4. سترى النتائج المرتبطة

### 3. اسأل الذكاء الاصطناعي
1. اذهب إلى قسم Chat
2. اكتب سؤالك
3. النموذج سيرد بناءً على الملفات المرفوعة

---

## 🛠️ أوامر مفيدة

### مراقبة الخدمات
```bash
# عرض السجلات الحية
docker-compose logs -f

# عرض سجلات خدمة محددة
docker-compose logs -f backend

# عرض آخر 50 سطر
docker-compose logs --tail 50
```

### إدارة الخدمات
```bash
# إعادة تشغيل جميع الخدمات
docker-compose restart

# إعادة خدمة محددة
docker-compose restart backend

# إيقاف مؤقت
docker-compose stop

# إيقاف نهائي مع حذف البيانات
docker-compose down -v
```

### استهلاك الموارد
```bash
# عرض استخدام CPU والذاكرة
docker stats

# عرض تفاصيل حاوية معينة
docker inspect vanand49d48x-rag-backend-1
```

---

## ⚠️ المشاكل الشائعة والحلول

### ❌ خطأ: Backend لا يبدأ

**الحل 1**: تحقق من السجلات
```bash
docker logs vanand49d48x-rag-backend-1
```

**الحل 2**: أعد التشغيل
```bash
docker-compose restart backend
```

**الحل 3**: إعادة البناء
```bash
docker-compose build --no-cache backend
docker-compose up -d backend
```

### ❌ خطأ: المنفذ مشغول

**السبب**: تطبيق آخر يستخدم نفس المنفذ

**الحل**:
```bash
# ابحث عن العملية
lsof -i :8000

# اقتل العملية (قد تحتاج sudo)
kill -9 <PID>

# أو غير المنفذ في docker-compose.yml
# ports:
#   - "8001:8000"
```

### ❌ خطأ: عدم الاتصال بـ LLM

**السبب**: llama.cpp لم يبدأ

**الحل**:
```bash
# تحقق من السجلات
docker logs vanand49d48x-rag-llama-cpp-1

# تأكد من وجود النموذج
ls -lh models/

# أعد التشغيل
docker-compose restart llama-cpp
```

### ❌ خطأ: Qdrant لا يستجيب

**الحل**:
```bash
# إعادة تشغيل
docker-compose restart qdrant

# أو إعادة كاملة مع حذف البيانات
docker-compose down -v
docker-compose up -d qdrant
```

### ❌ خطأ: مساحة قرص امتلأت

**الحل**:
```bash
# حذف الصور غير المستخدمة
docker system prune -a

# حذف المجلدات القديمة
rm -rf models/*.old

# حذف السجلات
rm -rf data/logs/*
```

---

## 📊 مراقبة الأداء

### في Grafana
1. افتح http://localhost:3000
2. اسم المستخدم: admin
3. كلمة المرور: admin
4. اختر لوحة تحكم (Dashboard)

### في Prometheus
1. افتح http://localhost:9090
2. اكتب metric (مثل: `container_memory_usage_bytes`)
3. اضغط "Graph"

---

## 🔐 الأمان والإنتاج

### للإنتاج - قم بـ:

```bash
# استخدم docker-compose.prod.yml
docker-compose -f docker-compose.prod.yml up -d

# غير كلمات المرور الافتراضية
# - Grafana: admin/admin → أنشئ قوية
# - غيّر مفاتيح API

# أضف HTTPS/SSL
# استخدم reverse proxy (nginx, traefik)

# فعّل Authentication
# في Backend API

# حدّد استهلاك الموارد
resources:
  limits:
    cpus: '4'
    memory: 12G
```

---

## 📝 ملفات التوثيق المتاحة

| الملف | الاستخدام |
|------|----------|
| **START_HERE.md** | البداية السريعة |
| **SUCCESS_SUMMARY.md** | ملخص الإنجازات |
| **FINAL_STATUS.md** | الحالة النهائية |
| **ADVANCED_ANALYSIS_AR.md** | تفاصيل معمارية |
| **COMMANDS_REFERENCE.md** | مرجع الأوامر |

---

## ✅ قائمة التحقق النهائية

```
[ ] تثبيت Docker و Docker Compose
[ ] الدخول للمجلد الصحيح
[ ] بناء Backend
[ ] بدء الخدمات
[ ] الانتظار 30 ثانية
[ ] فحص docker-compose ps
[ ] اختبار المنافذ الستة
[ ] فتح Dashboard
[ ] رفع ملف اختبار
[ ] البحث عن شيء
[ ] نجاح كامل! 🎉
```

---

## 🎯 الخطوات التالية بعد النجاح

### يوم الأول
- استكشف الواجهة
- جرب رفع أنواع ملفات مختلفة
- افهم الأداء

### الأسبوع الأول
- اضبط الإعدادات
- أضف المستخدمين
- راقب الأداء

### الشهر الأول
- تكامل مع أنظمة أخرى
- إضافة نماذج جديدة
- تحسينات الأمان

---

## 💬 الدعم والمساعدة

### المشاكل الشائعة
- تحقق من `COMMANDS_REFERENCE.md`
- ابحث في السجلات: `docker-compose logs`
- افحص الحالة: `bash check_status.sh`

### التوثيق الشاملة
- انظر إلى `docs/` folder
- اقرأ `README_INDEX.md`
- ابحث عن ملفات `.md` الأخرى

### المشاكل التقنية
```bash
# اجمع معلومات عن النظام
docker-compose ps
docker stats
docker logs -f

# شارك هذه المعلومات للدعم
```

---

## 🎉 نهاية الدليل!

**أنت الآن جاهز لتشغيل نظام Enterprise RAG! 🚀**

اتبع الخطوات أعلاه وستنجح في دقائق!

---

**آخر تحديث**: 2024-12-12  
**الحالة**: جاهز للإنتاج ✅  
**إصدار النظام**: 1.0  

**استمتع باستخدام نظام الذكاء الاصطناعي المتقدم! ✨**
